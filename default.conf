server {
    listen 80;
    listen 443;
    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback — $NGINX_BASE_PATH is substituted at container start via envsubst
    location $NGINX_BASE_PATH {
        try_files $uri $uri/ ${NGINX_BASE_PATH}index.html;
    }
}
