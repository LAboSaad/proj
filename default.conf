server {
    listen 80;
    listen 443;
    root /usr/share/nginx/html;
    index index.html;

    # Serve static asse
    # SPA fallback — all other routes serve index.html
    location / {
        try_files $uri $uri/ /index.html;
    }
}
